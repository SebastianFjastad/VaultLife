@model IEnumerable<Vaultlife.Models.Language>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create") &nbsp; @Html.ActionLink("Clone", "Clone")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LanguageName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LanguageTwoLetters)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LanguageThreeLetters)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LanguageName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LanguageTwoLetters)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LanguageThreeLetters)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LanguageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LanguageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LanguageID })
        </td>
    </tr>
}

</table>
